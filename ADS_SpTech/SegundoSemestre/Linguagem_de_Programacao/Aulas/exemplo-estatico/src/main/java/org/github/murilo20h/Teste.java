package org.github.murilo20h;

public class Teste {
    public static void main(String[] args) {
        //Esse método é estático, não precisa de instância
        Double resultadoPotencia = Math.pow(10, 5);

        Double resultadoSoma = Calculadora.soma(10.0,32.0);
    }
}